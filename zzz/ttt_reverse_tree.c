struct s_node {
	int				value;
	struct s_node	*right;
	struct s_node	*left;
};


void reverse_tree(struct s_node *root)
{
    rt(left);
    rt(right);

    // note, there is not if condition here!
    struct s_node *tmp = r->left
    r->left = r->right 
    r->right = tmp
}

// post

